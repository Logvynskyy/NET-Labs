using System;
using System.Collections.Generic;
//using NUnit.Framework;
//using NUnit;
//using NUnit.Compatibility;

namespace LinkedList.test
{
    //[TestFixture]
    //class ListTest
    //{
    //    [Test]
    //    public void insertionTest()
    //    {
    //        MyLinkedList<String> actual = new MyLinkedList<string>();
    //        Assert.AreEqual(actual.Count, 0);

    //        List<String> expected = new List<string>(new[] { "A", "B", "C" });

    //        foreach (var item in expected)
    //            actual.Add(item);

    //        Assert.AreEqual(3, actual.Count);
    //        Assert.AreEqual(actual, expected);
    //    }

    //    [Test]
    //    public void deletionTest()
    //    {
    //        MyLinkedList<String> actual = new MyLinkedList<string>();

    //        string[] expected = new string[] { "A", "B", "C", "D", "E" };

    //        foreach (var item in expected)
    //            actual.Add(item);

    //        Assert.True(actual.Remove(expected[0]));
    //        Assert.AreEqual(4, actual.Count);
    //        Assert.AreEqual(actual, new string[] { expected[1], expected[2], expected[3], expected[4] });

    //        Assert.True(actual.Remove(expected[4]));
    //        Assert.AreEqual(3, actual.Count);
    //        Assert.AreEqual(actual, new string[] { expected[1], expected[2], expected[3] });

    //        Assert.True(actual.Remove(expected[2]));
    //        Assert.AreEqual(2, actual.Count);
    //        Assert.AreEqual(actual, new string[] { expected[1], expected[3] });

    //        Assert.False(actual.Remove("F"));

    //        actual.Add(expected[0]);
    //        actual.Add(expected[0]);
    //        Assert.AreEqual(4, actual.Count);

    //        Assert.True(actual.Remove(expected[0]));
    //        Assert.AreEqual(3, actual.Count);
    //        Assert.AreEqual(actual, new string[] { expected[0], expected[1], expected[3] });

    //        Assert.True(actual.Remove(expected[0]));
    //        Assert.AreEqual(2, actual.Count);
    //        Assert.AreEqual(actual, new string[] { expected[1], expected[3] });
    //    }
    //}
}
